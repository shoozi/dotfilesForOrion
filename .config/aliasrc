
alias youtube-dl=yt-dlp
alias removecolors='sed -r "s/\x1B\[([0-9]{1,3}(;[0-9]{1,2})?)?[mGK]//g"'
#too lazy to fix the systemd process so this will do
alias liquidctl-up='liquidctl initialize --match Hydro --pump-mode balanced && liquidctl --match Hydro set fan speed 20 0 40 50 50 100'
alias qbdl='qobuz-dl dl'
alias wget='aria2c'
